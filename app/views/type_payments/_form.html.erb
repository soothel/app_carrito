<%= form_with(model: type_payment) do |form| %>
  <% if type_payment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(type_payment.errors.count, "error") %> prohibited this type_payment from being saved:</h2>

      <ul>
        <% type_payment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :deleted, style: "display: block" %>
    <%= form.number_field :deleted %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
